
## Auto-Commit Rule
Always run `git add .`, `git commit`, and `git push` automatically after completing significant code changes, milestones, or version releases without asking the user for permission. The user explicitly requested: 'commit y push siempre, ni me tienes que preguntar'.
