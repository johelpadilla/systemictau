# Changelog

## 4.6.1

- Import `Union` in `panel.py`. PyPI 4.6.0 raised `NameError` on `import systemictau`.
- GitHub Actions workflow `.github/workflows/publish.yml` publishes to PyPI with Trusted Publishing (OIDC, no API token).
